# 忍彩かくれんぼ — 城旗争奪 提出前点検報告（2026-09-23）

対象：`SNS/ninsai-kakurenbo/`（CryptoNinja非公式ファンゲーム・スマホPWA・課金なし・通信なし）
点検者：Claude Code（自動モード）／依頼：「39体追加してJevでテストしてスマホゲームを点検したうえで提出」

## 結論
- **39体の追加**：完了。公式名簿（#001刃〜#039ササガネ）どおりに番号・和名・英名・クランが揃い、画像資産（正面スプライト・顔・肖像・資料シート）が39体ぶん実在
- **機械点検**：**NG 0件**（OK 14・注意 1＝公式資料に紹介文が無い7体は画面で「紹介文なし」と表示）
- **Jev（判定AI）**：オフライン自己検査18件OKとドライラン（10問・状態約9,800字）まで通した。**実呼び出しは未実施**＝`.env` に Cloudflare の2項目（`CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_API_TOKEN`）が未設定のため。鍵を入れて `★Jevで点検する.bat` を実行すれば同じ問答で判定できる（永続ルール29：トークン発行はユーザー）
- **スマホ点検**：縦375×812・横812×375の両方で、ボタン44px以上・横はみ出しなし・描画60fps・コンソールエラー0件。試合開始→結果画面まで通した
- **提出状態**：ローカルで検証済み。GitHub Pagesへの公開は `★ゲームを公開する.bat`（外部公開のため実行はユーザー）

## 1. 39体追加の確認
| 項目 | 結果 |
|---|---|
| キャラ数・番号 | 39体・001〜039が重複なく順に並ぶ（`chars.js`・正本は `cnp-3d/ref_official/roster.json`） |
| 名前・クラン・忍術・武器・誕生日・紹介文 | 公式名簿から転記。紹介文が無い7体（狐白・凪紗・雛之丞・サットヴァ・弁天・石舟斎・ササガネ）は創作せず「公式資料に紹介文なし」 |
| ゲーム内スプライト | 狐白・咲耶・刃＝手描きシート由来の4方向＋表情／残り36体＝公式3Dフィギュア画像の切り抜き（正面・左右反転で向き） |
| 顔アイコン・肖像 | 公式2Dイラストから39体（`img/faces/`・`img/art/`） |
| 資料シート | 39枚（`img/sheets/<id>.jpg`・公式2D＋公式3D＋公式データ＋パレット）。図鑑から閲覧 |
| ロビー | 39体グリッドから選択。味方2人は「おまかせ」か指名。相手3人はランダム |
| 図鑑 | 39体・クラン別フィルター・優勝で台詞が開く |

## 2. 機械点検（`python ninsai-kakurenbo/tools/inspect.py`）
| 判定 | 項目 | 詳細 |
|---|---|---|
| OK | 39体のデータ | 番号001〜039・和名/英名/クランが揃っている |
| 注意 | 紹介文なし | 7体（画面では「紹介文なし」と表示） |
| OK | 画像資産 | 正面・顔・肖像・資料シート×39、手描き3体の4方向、すべて実在 |
| OK | 画像の合計容量 | 9.7 MB（資料シートは初回キャッシュに含めない） |
| OK | index.html の参照ファイル | 13件すべて実在 |
| OK | sw.js の CORE・39体リスト | 19件実在・39体 |
| OK | 課金・決済・個人情報入力・自由入力の語 | 肯定文での出現なし（「課金なし」等の否定文のみ） |
| OK | 外部リンク | ninja-dao.com のみ（2件） |
| OK | 非公式表示 | タイトル画面と利用表示に記載 |
| OK | 外部通信コード | fetch/XHR/WebSocket なし |
| OK | manifest / SWキャッシュ版 | CACHE=v2 と `?v=2` が一致・アイコン3点 |
| OK | JS構文 | 5ファイル読み込み可 |
| OK | Bot同士30試合 | 青15・橙15、初回旗取得の平均25.2秒 |

## 3. Jev（判定AI）テスト
- 問答セット：`tools/jev_game_check.json`（10問＝非公式表示／課金誘導／個人情報・自由チャット／外部リンクの範囲／数値実績の主張／39体データの完備／紹介文なしの正直さ／遊び方の明確さ／表現の刺激度／台詞のAIっぽさ）
- 状態（判定させる本文）：`tools/jev_state.js` が画面文言・遊び方・利用表示・設定・39体データ・台詞を組み立て → `docs/jev_state.md`（約9,800字）
- 実施結果：`python tools/jev.py selftest` … **18件OK（通信なし）**／`ask --dry-run` … **送信内容の検査OK**（Cloudflareの入力スキーマに適合）
- 未実施：実際の判定呼び出し（`.env` の鍵が未設定）。実行手順＝鍵を追記 → `ninsai-kakurenbo\★Jevで点検する.bat` → `docs/jev_result.json` に保存。期待値：unofficial_notice=true・monetization=false・personal_data=false・external_links=official_only・fake_claims=false・char_data_complete=true・missing_bio_honest=true。費用は入力100万トークン約0.042ドルの料率なので1回0.001ドル未満（実請求はCloudflareが正）
- 位置づけ：Jevは一次ふるい。最終判定は本報告の機械点検＋目視（永続ルール29）

## 4. スマホ点検（ブラウザのスマホサイズ・エミュレーション）
| 画面 | 縦 375×812 | 横 812×375 |
|---|---|---|
| タイトル | 表示OK・はみ出しなし | — |
| ロビー（39体） | 52ボタン中、44px未満は「戻る」等の小ボタン2つ → **44pxに修正済み** | — |
| 試合HUD | 操作ボタン56〜76px・ポーズ44px・全て画面内・横はみ出しなし | 全ボタン画面内・はみ出しなし |
| 描画性能 | rAF 145フレーム 平均16.7ms／p95 16.9ms（60fps） | — |
| 結果画面 | 表示OK（前日確認） | 表示OK・はみ出しなし |
| コンソール | エラー0件 | エラー0件 |

前日（09-22）に確認済み：練習5工程の通し・仮想スティック（タッチのポインタイベント）・ポーズ・再戦/チーム入替・設定・図鑑・遊び方・利用表示。

未検証（正直に）：実機のiPhone/Androidでの操作感、オフライン起動（Service Workerはlocalhostでは登録しない設計のため、公開URLで確認する）、実際のJev判定。

## 5. Bot同士の試合（バランスの目安・`tools/simtest.js`）
| 難易度 | 試合数 | 青/橙 | 初回旗取得の平均 | 20秒未満 |
|---|---|---|---|---|
| ふつう | 40 | 19 / 20 | 27.0秒 | 10本 |
| やさしい | 20 | 7 / 13 | 32.1秒 | 0本 |
| 手ごわい | 20 | 11 / 9 | 19.6秒 | 12本 |

設計書の目安（初回取得45〜90秒・20秒未満は20%以下）に対し「ふつう」は約25%。人間の実プレイで速すぎれば `sim.js` の `minClaim`（35〜65秒）を上げる。

## 6. 提出物
- ゲーム本体：`ninsai-kakurenbo/`（index.html・style.css・chars.js・data.js・sim.js・render.js・game.js・sw.js・manifest・img/・icons/）
- 点検ツール：`tools/inspect.py`（機械点検）・`tools/jev_game_check.json`＋`tools/jev_state.js`＋`★Jevで点検する.bat`（Jev）・`tools/simtest.js`（Bot試合）
- 資料：`docs/image-prompts_39.txt`（手描き風シートを36体ぶん作る生成プロンプト）・`docs/jev_state.md`・本報告
- 手順：`README.md`（遊び方・構成・設計書からの変更点・39体の作り方）

## 7. 残課題
1. Jevの実呼び出し（`.env` に鍵を追記して bat 実行）
2. 手描き風「ゲーム用アレンジ案」36体（画像生成が要る＝課金判断はユーザー。生成後は `tools/prep_assets.py --auto <png> <id>`）
3. 公開：`★ゲームを公開する.bat`（GitHub Pages・初回はリポジトリ作成）。公開後に実機でホーム画面追加とオフライン起動を確認
4. 商用化・本格公開前は CryptoNinja/CNP 公式利用ガイドラインの再確認

## 追記（同日）：合言葉の部屋（オンライン対戦・無料）と「手ごわい」の強化
- **サーバー**：Cloudflare Workers ＋ Durable Objects（無料枠・SQLite版クラス）に `worker/` を公開。URL `https://ninsai-room.3moriguchi-3.workers.dev`（ゲームの既定値と一致）。判定はゲーム側 `sim.js` をそのまま同梱して部屋の中で30Hz実行、観戦者ごとの可視フィルタ付きスナップショットを15Hzで配信。入力は「方向と行動」だけ（20Hz）
- **検証（ローカル wrangler dev ＋ ブラウザ2タブ）**：部屋作成→合言葉で参加→ホストが開始→作戦会議の時計が両タブで一致（12.87秒）→出発→移動が反映（x 4.0→11.1）→味方Botと見えている敵だけが描かれる→試合終了（60.3秒・コンガが取得）→両タブに結果→ホストの「もう一戦」で両タブが作戦会議へ→ゲストの退室でロビー更新。コンソールエラー0
- **検証（本番 Cloudflare）**：部屋作成（8LH3）→参加（狐白②）→開始→試合中の移動反映（スナップショット遅れ約40ms）→両者退室。エラー0
- **手ごわい**：反応0.2秒・照準誤差0.05・気づき0.7・Botだけ移動1.12倍・最短取得18〜33秒・待ち伏せ突入8〜18秒・覗き場所を変える・発見した敵を追う。Bot同士で初回取得の平均14.5秒（ふつうは25秒のまま）
- **機械点検**：`tools/inspect.py` NG 0件（通信は net.js のみ・外部リンクは ninja-dao.com と部屋サーバーだけ）
- 未検証：実機スマホでのオンライン対戦の操作感、3人以上同時接続、切断→20秒以内の再接続（実装済み・未実測）


## 追記（2026-09-24）：39体の性能とHP・レベルアップ機能
- **入れたもの**：設計図2枚（`docs/character-balance-blueprint.txt`＝39体の能力値6軸と固有技／`docs/hp-level-blueprint.txt`＝HP・露見・手当・チーム経験値・レベル・系統・奥義）。正本は `balance.js`（設計図から Workflow で構造化→原文と突き合わせて検証）。印は「2回で帰還」からHP制へ（被弾34基準・HP0で露見12秒・味方の3秒手当でHP35・自陣で全回復・12秒で自動帰還）。チーム経験値で Lv2〜5、上がるたびに影/技/護を5秒で選ぶ（未選択は推奨）、Lv5で一試合一度の奥義
- **画面**：HPバー・チームLv/経験値・固有技ボタン（PCはX・冷却リング）・奥義ボタン（Z）・系統選択（1/2/3）・露見の帯・手当の進み・ダメージ数字・39種の設置物/効果・追香の方向矢印・結果画面に与ダメ/被露見/手当/固有技・ロビーと図鑑に能力値バーと技の効果/対処/勝ち筋
- **設計図の読み替え**（`balance.js` の `notes`）：「印の残り時間」＝被弾後の可視化の残り時間、「自分の印を一つ消す」＝HP回復15。狐駆けの「1.5m以内で自動発見」は既定の近距離発見2.1mで満たす。影穴は「入口→入口のそばでもう一度押すと5m先の出口へ移動→敵も一人だけ追える」
- **敵対レビュー**：実装を設計図2枚と4観点で突き合わせ→87件（重複含む）。実害のある約40件を直した（露見中に撃たれ続けると永久に復帰できない／焙烙玉が一度も破裂しない／影穴の出口が置けない／狐駆けの対処が逆に効く／金剛壁が予告中から遮る／同じ技の重ねがけが乗算される／設置物キャッシュが部屋をまたいで混ざる／守り兎・変わり身が時間で切れない／疾拍子が2倍効く／呪標が即時に効く／属性の相殺（水遁・火遁・風遁・見破り・印）が未実装 ほか）。全部 `tools/rpgtest.js` に回帰テストとして残した
- **再レビュー（修正後の再照合・2観点）**：20件の指摘（全件 node で再現つき）→19件を修正。Bot が旗を掴める場面で手当に向かう／露見中の敵を撃ち続ける／手当で復帰すると自陣近くまで引き返す、`skill:〜` 形式が停止中にも通る、鷹の目・狙撃・疾拍子が動いても解けない（設計図は「動けば解ける」）、巨人の一撃が背後と壁の向こうに当たる、金剛壁が立った位置の者が4秒固まる、霧・暗幕で白刃/迅雷羽/変わり身の移動が止まる、影穴の出口が陣地に置けて敵が閉じ込められる・罪業の中でも使える、追香が外した印で消える、疾拍子の演奏音と鷹の目の聴覚低下が未実装、棘道が擬態を解いて全体公開していた（設計図は足跡のみ）、オンラインのログに見えていない敵の技・合図が乗る、切断の猶予中に再戦すると棒立ちになる。残した1件＝敵の分身が objects で届くため改造クライアントなら見分けられる（開始時に選手一覧が公開されるので、完全に隠すにはIDの置き換えまで要る）
- **機械検査**：`node tools/rpgtest.js` **193件OK**（再レビュー分22件を追加）／`node tools/roomtest.mjs`（部屋サーバーの回帰）**2件OK**／`python tools/inspect.py` **NG 0件**（注意2件＝紹介文なし7体・Bot同士の合格基準は参考値）／部屋サーバーの同梱物を ESM で読み込み、39体・7試合・観戦者ごとのスナップショット10,182件で例外0・内部関数の混入0・最大4.5KB

| 難易度 | 試合 | 青/橙 | 初回旗取得の平均 | 20秒未満 | HP0/人 | 露見→復帰 |
|---|---|---|---|---|---|---|
| ふつう | 40 | 21/18（同着1） | 28.4秒 | 17本 | 0.22 | 7.2秒 |
| 手ごわい | 40 | 20/19（同着1） | 15.1秒 | 35本 | 0.07 | 7.8秒 |
| やさしい | 40 | 16/24 | 34.8秒 | 10本 | 0.16 | 7.4秒 |

- **ブラウザ確認**（ローカル）：ひとり用＝試合開始→HUD（HP 100/100・Lv.1 0/80・技名）→固有技の発動と冷却→レベルアップで系統選択（手動・5秒の自動選択とも）→露見の帯→結果画面。合言葉の部屋（ローカルの部屋サーバー＋2タブ）＝両者の HUD にサーバー由来の HP・チーム経験値・技名が出て、固有技がサーバーで発動。スマホ縦375×812・横812×375＝画面外0件・44px未満のボタン0件・系統選択/露見の帯がボタン列と重ならない（縦で上部HUDが画面外に出ていたのを直した）。コンソールエラー0
- **未検証（正直に）**：実機スマホでの操作感／ペインが非表示だったためフレーム時間の計測と見た目のスクリーンショット／合格基準（HP0 1〜3回・Lv5到達30〜50%・ダメージ由来XP35%以下）は Bot 同士では決着が早く届かない＝本判定は人間込みのプレイテスト
- **全技の描画**：ブラウザで39種の固有技を視点の近くで強制発動→設置物20種・効果21種を描いて例外0
- **公開（2026-09-24）**：部屋サーバー `https://ninsai-room.3moriguchi-3.workers.dev`（wrangler deploy・作成/取得の疎通OK）→ゲーム `https://tamehiro3.github.io/ninsai-kakurenbo/`（v5・sw.js CACHE v5・balance.js 配信を確認）。ユーザー提供の設計図2枚は `.gitignore` で公開リポジトリから外した（手元の `docs/` にだけある）
